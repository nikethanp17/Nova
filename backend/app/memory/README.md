# app/memory/

Hosts persistent non-sensitive memory layers mapping personal context, task logs, and preferences.

## Directory Responsibilities

- `memory_manager.py`: CRUD interfaces mapping database reads.
- `categories.py`: Declares categorization settings for user context mapping.
- `vector_search.py`: Implements vector search indexes mapping semantic user records.
